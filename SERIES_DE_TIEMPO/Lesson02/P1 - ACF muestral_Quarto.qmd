---
title: "P1 - ACF (AUTOCOVARIANZA) muestral - SERIES DE TIEMPO UNIVARIADAS"
author: "Brayan Cubides"

format:
  html:
    theme: cosmo
    page-layout: full
    toc: true
    toc-location: left
    toc-depth: 2
    #number-sections: true
    code-tools: true
    lightbox: true
    self-contained: true 
---

## ACF muestral de un ruido blanco wt

El Autocorrelograma decae rápidamente, por lo cual no hay estacionariedad

```{r}
rm(list = ls(all = TRUE))

# Simula un modelo ARIMA(p,d,q) si d=0, es un ARMA(p,q)
rt_RB <- arima.sim(list(order = c(0,0,0)), n = 1000 )
par(mfrow = c(1,2))
plot(rt_RB, type = "l", main = "Serie simulada ruido blanco wt")
acf(rt_RB, lag.max = 40) 
```

## ACF muestral de una Caminata Aleatoria

### Generación de la serie con y sin drift

```{r}
set.seed(154) # para reproducir los resultados
w  <- rnorm(200); x  <- cumsum(w)   # dos comandos en una sola línea
wd <- w + 0.2;    xd <- cumsum(wd)
par(mfrow = c(1,1))
plot.ts(xd, ylim = c(-5,55), main = "Random walk", ylab = "")
lines(x, col = 4)
abline(h = 0, col = 4, lty = 2)
abline(a = 0, b = 0.2, lty = 2)
```

### ACF de la caminata aleatoria sin drift

```{r}
par(mfrow = c(2,2))
plot.ts(x, ylim = c(-5,55), main = "Random walk sin drift", ylab = "", col = 4)
abline(h = 0, col = 4, lty = 2)
acf(x, lag.max = 40)
```

### ACF de la caminata aleatoria con drift

```{r}
plot.ts(xd, main = "Random walk con drift", ylab = "")
abline(a = 0, b = 0.2, lty = 2)
acf(xd, lag.max = 40)
```

## ACF de una serie con estacionalidad (AirPassengers)

Se logra ver tendencia en el Autocorrelograma

```{r}
par(mfrow = c(1,2))
plot(AirPassengers)
acf(AirPassengers, lag.max = 48) 
```
